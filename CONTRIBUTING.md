# Contributing

感谢你考虑改进 `my-spec`。

## 修改原则

- 保持 skill 目标明确：帮助 Codex 生成项目一级计划和实施细则。
- 优先改进模板、边界条件和提问规则，不要加入和项目计划无关的工作流。
- 修改 `SKILL.md` 时，请同时检查 `references/` 下的模板是否需要同步。
- 不要提交用户本地产生的 `.codex/` 计划文件、密钥、账号配置或私有项目内容。

## 推荐流程

1. Fork 本仓库并创建分支。
2. 修改相关文件。
3. 手动检查 `SKILL.md`、模板和 README 描述是否一致。
4. 提交 pull request，并说明变更动机、影响范围和验证方式。

## Pull Request 要求

- 描述这次改动解决了什么问题。
- 说明是否影响已有 skill 调用方式。
- 如果改了模板，请给出一个简短示例或说明预期输出变化。
