# Keep repository text files normalized for GitHub and cross-platform use.
* text=auto eol=lf

# Shell/batch files can opt into platform-specific endings if added later.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
