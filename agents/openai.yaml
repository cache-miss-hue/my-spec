interface:
  display_name: "My Spec"
  short_description: "Clarify projects into plans and implementation specs"
  default_prompt: "Use $my-spec to interview me until the project is clear, then create a first plan."

metadata:
  author: "cache-miss-hue"
  version: "0.1"
